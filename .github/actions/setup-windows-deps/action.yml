name: Setup Windows dependencies
description: Install shared Windows CI dependencies and build cached third-party libraries

inputs:
  ffmpeg-ref:
    description: FFmpeg ref to checkout
    required: false
    default: n9.0
  use-ffmpeg:
    description: Whether to build and install FFmpeg
    required: false
    default: 'true'
  ffmpeg-cache-suffix:
    description: Cache suffix appended after the FFmpeg ref
    required: false
    default: full-v5-clang
  ffmpeg-configure:
    description: Shell snippet that configures FFmpeg before building
    required: true
  mimalloc-ref:
    description: mimalloc ref to checkout
    required: false
    default: v3.5.1
  use-mimalloc:
    description: Whether to build and install mimalloc
    required: false
    default: 'true'
  obuparse-ref:
    description: obuparse ref to checkout
    required: false
    default: v2.0.2
  use-obuparse:
    description: Whether to build and install obuparse
    required: false
    default: 'true'
  obuparse-cache-suffix:
    description: Cache suffix appended after the obuparse ref
    required: false
    default: clang-v1
  lsmash-repository:
    description: L-SMASH repository to checkout
    required: false
    default: vimeo/l-smash
  use-lsmash:
    description: Whether to build and install L-SMASH
    required: false
    default: 'true'
  lsmash-ref:
    description: L-SMASH ref to checkout
    required: false
    default: 4a21f2155b59d7176094db10dec7bbc274a4b9f3
  lsmash-cache-suffix:
    description: Cache suffix appended after the L-SMASH ref
    required: false
    default: clang-coff-refptr-v2
  lsmash-path:
    description: L-SMASH checkout path
    required: false
    default: l-smash
  lsmash-patch-path:
    description: Patch path relative to the checked-out L-SMASH directory
    required: false
    default: ../x265/.github/patches/l-smash-clang-coff-refptr.patch
  lsmash-patch-check-paths:
    description: Paths passed to git diff --check --cached after applying the L-SMASH patch
    required: false
    default: codecs/av1_obu.c codecs/description.c codecs/hevc.c codecs/mp4a.c codecs/mp4sys.c codecs/nalu.c core/box.c core/box_default.c core/box_default.h core/isom.c core/print.c core/read.c core/write.c
  lsmash-extra-cflags:
    description: Extra CFLAGS passed to L-SMASH configure
    required: false
    default: -I/usr/local/include -O3 -DNDEBUG -fno-lto -fno-pic -fno-pie
  lsmash-extra-ldflags:
    description: Extra LDFLAGS passed to L-SMASH configure
    required: false
    default: -L/usr/local/lib -fno-lto -fno-pic
  lsmash-install-prefix:
    description: Install prefix passed to L-SMASH configure
    required: false
    default: /usr/local
  lsmash-cc:
    description: C compiler passed to L-SMASH configure
    required: false
    default: clang
  use-gop-muxer:
    description: Whether to build and install GOP muxer
    required: false
    default: 'false'
  gop-muxer-repository:
    description: GOP muxer repository to checkout
    required: false
    default: msg7086/gop_muxer
  gop-muxer-ref:
    description: GOP muxer ref to checkout
    required: false
    default: 5677cf5ef905c2412ed31de300cd1a08b341d21d
  gop-muxer-cache-suffix:
    description: Cache suffix appended after the GOP muxer ref
    required: false
    default: lsmash-add-box-v2-clang-gnu20
  gop-muxer-path:
    description: GOP muxer checkout path
    required: false
    default: gop_muxer
  gop-muxer-patch-path:
    description: Patch path relative to the checked-out GOP muxer directory
    required: false
    default: ../x265/.github/patches/gop-muxer-lsmash-add-box.patch
  extra-msys2-packages:
    description: Extra MSYS2 packages to install
    required: false
    default: ''

runs:
  using: composite
  steps:
    - name: Cache FFmpeg
      if: inputs.use-ffmpeg == 'true'
      id: cache-ffmpeg
      uses: actions/cache@v5
      with:
        path: ffmpeg-install
        key: ffmpeg-${{ inputs.ffmpeg-ref }}-${{ inputs.ffmpeg-cache-suffix }}

    - name: Cache Obuparse
      if: inputs.use-obuparse == 'true'
      id: cache-obuparse
      uses: actions/cache@v5
      with:
        path: obuparse-install
        key: obuparse-${{ inputs.obuparse-ref }}-${{ inputs.obuparse-cache-suffix }}

    - name: Cache L-SMASH
      if: inputs.use-lsmash == 'true'
      id: cache-lsmash
      uses: actions/cache@v5
      with:
        path: lsmash-install
        key: lsmash-${{ inputs.lsmash-repository }}-${{ inputs.lsmash-ref }}-${{ inputs.lsmash-cache-suffix }}

    - name: Cache GOP muxer
      if: inputs.use-gop-muxer == 'true'
      id: cache-gop-muxer
      uses: actions/cache@v5
      with:
        path: gop-muxer-install
        key: gop-muxer-${{ inputs.gop-muxer-repository }}-${{ inputs.gop-muxer-ref }}-${{ inputs.gop-muxer-cache-suffix }}

    - name: Checkout FFmpeg
      if: inputs.use-ffmpeg == 'true' && steps.cache-ffmpeg.outputs.cache-hit != 'true'
      uses: actions/checkout@v6
      with:
        path: ffmpeg
        repository: FFmpeg/FFmpeg
        ref: ${{ inputs.ffmpeg-ref }}
        fetch-depth: 1

    - name: Checkout mimalloc
      if: inputs.use-mimalloc == 'true'
      uses: actions/checkout@v6
      with:
        path: mimalloc
        repository: microsoft/mimalloc
        ref: ${{ inputs.mimalloc-ref }}
        fetch-depth: 1

    - name: Checkout Obuparse
      if: inputs.use-obuparse == 'true' && steps.cache-obuparse.outputs.cache-hit != 'true'
      uses: actions/checkout@v6
      with:
        path: obuparse
        repository: dwbuiten/obuparse
        ref: ${{ inputs.obuparse-ref }}
        fetch-depth: 1
        sparse-checkout: |
          Makefile
          obuparse.c
          obuparse.h
        sparse-checkout-cone-mode: false

    - name: Checkout L-SMASH
      if: inputs.use-lsmash == 'true' && steps.cache-lsmash.outputs.cache-hit != 'true'
      uses: actions/checkout@v6
      with:
        path: ${{ inputs.lsmash-path }}
        repository: ${{ inputs.lsmash-repository }}
        ref: ${{ inputs.lsmash-ref }}
        fetch-depth: 1
        sparse-checkout: |
          Makefile
          configure
          lsmash.h
          liblsmash.v
          cli
          codecs
          common
          core
          importer
        sparse-checkout-cone-mode: false

    - name: Checkout GOP muxer
      if: inputs.use-gop-muxer == 'true' && steps.cache-gop-muxer.outputs.cache-hit != 'true'
      uses: actions/checkout@v6
      with:
        path: ${{ inputs.gop-muxer-path }}
        repository: ${{ inputs.gop-muxer-repository }}
        ref: ${{ inputs.gop-muxer-ref }}
        fetch-depth: 1
        sparse-checkout: |
          gop_muxer.cpp
          gop_muxer.h
        sparse-checkout-cone-mode: false

    - name: Setup MSYS2
      uses: msys2/setup-msys2@v2
      with:
        release: false
        msystem: CLANG64
        update: true
        install: >-
          base-devel
          git
          libtool
          mingw-w64-clang-x86_64-nasm
          mingw-w64-clang-x86_64-clang
          mingw-w64-clang-x86_64-compiler-rt
          mingw-w64-clang-x86_64-lld
          mingw-w64-clang-x86_64-crt
          mingw-w64-clang-x86_64-headers
          mingw-w64-clang-x86_64-libc++
          mingw-w64-clang-x86_64-libwinpthread
          mingw-w64-clang-x86_64-make
          mingw-w64-clang-x86_64-pkgconf
          mingw-w64-clang-x86_64-python
          mingw-w64-clang-x86_64-cmake
          mingw-w64-clang-x86_64-ninja
          mingw-w64-clang-x86_64-llvm
          ${{ inputs.extra-msys2-packages }}

    - name: Verify MSYS2 Toolchain
      shell: msys2 {0}
      run: |
        set -euo pipefail
        case "${MSYSTEM:-}" in
          CLANG64) ;;
          *) echo "Unexpected MSYSTEM: ${MSYSTEM:-unset}" >&2; exit 1 ;;
        esac
        echo "=== Dependency provenance ==="
        echo "ffmpeg=${{ inputs.ffmpeg-ref }} cache=ffmpeg-${{ inputs.ffmpeg-ref }}-${{ inputs.ffmpeg-cache-suffix }} enabled=${{ inputs.use-ffmpeg }}"
        echo "mimalloc=${{ inputs.mimalloc-ref }} enabled=${{ inputs.use-mimalloc }}"
        echo "obuparse=${{ inputs.obuparse-ref }} cache=obuparse-${{ inputs.obuparse-ref }}-${{ inputs.obuparse-cache-suffix }} enabled=${{ inputs.use-obuparse }}"
        echo "lsmash=${{ inputs.lsmash-repository }}@${{ inputs.lsmash-ref }} suffix=${{ inputs.lsmash-cache-suffix }} patch=${{ inputs.lsmash-patch-path }} cache=lsmash-${{ inputs.lsmash-repository }}-${{ inputs.lsmash-ref }}-${{ inputs.lsmash-cache-suffix }} enabled=${{ inputs.use-lsmash }}"
        echo "gop_muxer=${{ inputs.gop-muxer-repository }}@${{ inputs.gop-muxer-ref }} suffix=${{ inputs.gop-muxer-cache-suffix }} patch=${{ inputs.gop-muxer-patch-path }} enabled=${{ inputs.use-gop-muxer }} cache=gop-muxer-${{ inputs.gop-muxer-repository }}-${{ inputs.gop-muxer-ref }}-${{ inputs.gop-muxer-cache-suffix }}"
        echo "=== Toolchain paths and versions ==="
        for tool in clang c++ ld.lld llvm-ar llvm-ranlib llvm-profdata cmake ninja pkg-config; do
          tool_path=$(command -v "$tool")
          case "$tool_path" in
            /clang64/bin/*|/usr/bin/*) ;;
            *) echo "Unexpected $tool path: $tool_path" >&2; exit 1 ;;
          esac
          printf '%s=%s\n' "$tool" "$tool_path"
          "$tool" --version | head -1
        done
        echo "=== MSYS2 package provenance ==="
        pacman -Q \
          mingw-w64-clang-x86_64-clang \
          mingw-w64-clang-x86_64-lld \
          mingw-w64-clang-x86_64-llvm \
          mingw-w64-clang-x86_64-libc++ \
          mingw-w64-clang-x86_64-compiler-rt
        if [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
          {
            echo "### Windows dependency provenance"
            echo
            echo "| dependency | ref | cache | suffix | patch | enabled |"
            echo "| --- | --- | --- | --- | --- | --- |"
            echo "| FFmpeg | ${{ inputs.ffmpeg-ref }} | ffmpeg-${{ inputs.ffmpeg-ref }}-${{ inputs.ffmpeg-cache-suffix }} | ${{ inputs.ffmpeg-cache-suffix }} | n/a | ${{ inputs.use-ffmpeg }} |"
            echo "| mimalloc | ${{ inputs.mimalloc-ref }} | n/a | n/a | n/a | ${{ inputs.use-mimalloc }} |"
            echo "| obuparse | ${{ inputs.obuparse-ref }} | obuparse-${{ inputs.obuparse-ref }}-${{ inputs.obuparse-cache-suffix }} | ${{ inputs.obuparse-cache-suffix }} | n/a | ${{ inputs.use-obuparse }} |"
            echo "| L-SMASH | ${{ inputs.lsmash-repository }}@${{ inputs.lsmash-ref }} | lsmash-${{ inputs.lsmash-repository }}-${{ inputs.lsmash-ref }}-${{ inputs.lsmash-cache-suffix }} | ${{ inputs.lsmash-cache-suffix }} | ${{ inputs.lsmash-patch-path }} | ${{ inputs.use-lsmash }} |"
            echo "| GOP muxer | ${{ inputs.gop-muxer-repository }}@${{ inputs.gop-muxer-ref }} | gop-muxer-${{ inputs.gop-muxer-repository }}-${{ inputs.gop-muxer-ref }}-${{ inputs.gop-muxer-cache-suffix }} | ${{ inputs.gop-muxer-cache-suffix }} | ${{ inputs.gop-muxer-patch-path }} | ${{ inputs.use-gop-muxer }} |"
            echo
          } >> "$GITHUB_STEP_SUMMARY"
        fi

    - name: Compile FFmpeg
      if: inputs.use-ffmpeg == 'true' && steps.cache-ffmpeg.outputs.cache-hit != 'true'
      shell: msys2 {0}
      working-directory: ffmpeg
      run: |
        ${{ inputs.ffmpeg-configure }}
        make -j$(nproc)

    - name: Remove .dll.a Import Libraries
      shell: msys2 {0}
      run: |
        deleted=0
        for root in /clang64 /mingw64 /ucrt64 /clangarm64 /usr/local; do
          [ -d "$root" ] || continue
          while IFS= read -r -d '' file; do
            printf '%s\n' "$file"
            rm -f "$file"
            deleted=$((deleted + 1))
          done < <(find "$root" -type f -name "*.dll.a" -print0)
        done
        echo "Removed .dll.a import libraries: $deleted"

    - name: Install FFmpeg
      if: inputs.use-ffmpeg == 'true' && steps.cache-ffmpeg.outputs.cache-hit != 'true'
      shell: msys2 {0}
      working-directory: ffmpeg
      run: |
        make install
        mkdir -p ffmpeg-install/include ffmpeg-install/lib/pkgconfig ffmpeg-install/bin /clang64/bin
        for dir in /usr/local/include/libav* /usr/local/include/libsw*; do
          [ -d "$dir" ] || continue
          cp -R "$dir" ffmpeg-install/include/
        done
        for file in /usr/local/lib/libav*.a /usr/local/lib/libsw*.a; do
          [ -f "$file" ] || continue
          cp "$file" ffmpeg-install/lib/
        done
        for file in /usr/local/lib/pkgconfig/libav*.pc /usr/local/lib/pkgconfig/libsw*.pc; do
          [ -f "$file" ] || continue
          cp "$file" ffmpeg-install/lib/pkgconfig/
        done
        for file in /usr/local/bin/ffmpeg* /usr/local/bin/ffprobe*; do
          [ -f "$file" ] || continue
          install -v "$file" /clang64/bin/
          cp "$file" ffmpeg-install/bin/
        done
        mv ffmpeg-install "$GITHUB_WORKSPACE/"

    - name: Install Cached FFmpeg
      if: inputs.use-ffmpeg == 'true' && steps.cache-ffmpeg.outputs.cache-hit == 'true'
      shell: msys2 {0}
      run: |
        cp -R ffmpeg-install/include/. /usr/local/include/
        cp -R ffmpeg-install/lib/. /usr/local/lib/
        if [ -d ffmpeg-install/bin ]; then
          mkdir -p /usr/local/bin /clang64/bin
          cp -R ffmpeg-install/bin/. /usr/local/bin/
          cp -R ffmpeg-install/bin/. /clang64/bin/
        fi

    - name: Verify FFmpeg Install
      if: inputs.use-ffmpeg == 'true'
      shell: msys2 {0}
      run: |
        for file in \
          /usr/local/lib/libavformat.a \
          /usr/local/lib/libavcodec.a \
          /usr/local/lib/libavutil.a \
          /usr/local/lib/libswscale.a \
          /usr/local/lib/pkgconfig/libavformat.pc; do
          test -s "$file"
          ls -l "$file"
        done
        for dir in \
          /usr/local/include/libavcodec \
          /usr/local/include/libavformat \
          /usr/local/include/libavutil; do
          test -d "$dir"
          ls -ld "$dir"
        done
        if [ -f /usr/local/lib/pkgconfig/libswscale.pc ]; then
          ls -l /usr/local/lib/pkgconfig/libswscale.pc
        else
          echo "::warning::FFmpeg libswscale.pc is missing; static LAVF discovery may rely on aggregate pkg-config metadata"
        fi
        if [ -x /clang64/bin/ffmpeg ]; then
          /clang64/bin/ffmpeg -hide_banner -version | head -1
        else
          echo "FFmpeg CLI not installed for this cache profile"
        fi
        if [ -x /clang64/bin/ffprobe ]; then
          /clang64/bin/ffprobe -hide_banner -version | head -1
        fi

    - name: Compile mimalloc
      if: inputs.use-mimalloc == 'true'
      shell: msys2 {0}
      working-directory: mimalloc
      run: |
        mkdir build
        cd build
        cmake -GNinja .. -DCMAKE_INSTALL_PREFIX=/usr/local \
          -DCMAKE_INTERPROCEDURAL_OPTIMIZATION=ON \
          -DMI_BUILD_SHARED=OFF -DMI_BUILD_STATIC=ON -DMI_BUILD_OBJECT=OFF \
          -DMI_BUILD_TESTS=OFF -DMI_USE_CXX=ON
        ninja
        ninja install
        shopt -s nullglob
        if [ ! -s /usr/local/include/mimalloc.h ]; then
          for file in /usr/local/include/mimalloc-*/mimalloc.h; do
            install -v "$file" /usr/local/include/mimalloc.h
            break
          done
        fi
        if [ ! -s /usr/local/lib/libmimalloc.a ]; then
          for file in /usr/local/lib/mimalloc-*/libmimalloc.a; do
            install -v "$file" /usr/local/lib/libmimalloc.a
            break
          done
        fi
        for dir in /usr/local/lib/cmake/mimalloc-*; do
          rm -f /usr/local/lib/cmake/mimalloc
          ln -s "$dir" /usr/local/lib/cmake/mimalloc
          break
        done

    - name: Verify mimalloc Install
      if: inputs.use-mimalloc == 'true'
      shell: msys2 {0}
      run: |
        test -s /usr/local/include/mimalloc.h
        test -s /usr/local/lib/libmimalloc.a
        test -d /usr/local/lib/cmake
        find /usr/local/lib/cmake -maxdepth 2 \( -name 'mimalloc*.cmake' -o -path '*/mimalloc-*' \) | grep -q .

    - name: Compile Obuparse
      if: inputs.use-obuparse == 'true' && steps.cache-obuparse.outputs.cache-hit != 'true'
      shell: msys2 {0}
      working-directory: obuparse
      run: |
        make CC=cc libobuparse.a
        install -v obuparse.h /usr/local/include
        install -v libobuparse.a /usr/local/lib
        mkdir -p obuparse-install/include obuparse-install/lib
        cp /usr/local/include/obuparse.h obuparse-install/include/
        cp /usr/local/lib/libobuparse.a obuparse-install/lib/
        mv obuparse-install "$GITHUB_WORKSPACE/"

    - name: Install Cached Obuparse
      if: inputs.use-obuparse == 'true' && steps.cache-obuparse.outputs.cache-hit == 'true'
      shell: msys2 {0}
      run: |
        cp -R obuparse-install/include/. /usr/local/include/
        cp -R obuparse-install/lib/. /usr/local/lib/

    - name: Verify Obuparse Install
      if: inputs.use-obuparse == 'true'
      shell: msys2 {0}
      run: |
        test -s /usr/local/include/obuparse.h
        test -s /usr/local/lib/libobuparse.a

    - name: Compile L-SMASH
      if: inputs.use-lsmash == 'true' && steps.cache-lsmash.outputs.cache-hit != 'true'
      working-directory: ${{ inputs.lsmash-path }}
      env:
        MSYSTEM: CLANG64
      shell: msys2 {0}
      run: |
        set -x
        export PATH="/clang64/bin:/usr/bin"
        hash -r
        clang --version

        for file in ${{ inputs.lsmash-patch-check-paths }}; do
          test -f "$file"
        done
        echo "Validated L-SMASH patch targets: ${{ inputs.lsmash-patch-check-paths }}"
        git -c core.autocrlf=false reset --hard HEAD
        git apply --ignore-whitespace --check ${{ inputs.lsmash-patch-path }}
        git apply --ignore-whitespace ${{ inputs.lsmash-patch-path }}
        git diff --check -- ${{ inputs.lsmash-patch-check-paths }}
        grep -Fq 'lsmash_local_isom_box_type' codecs/description.c
        grep -Fq "LSMASH_4CC( 'h', 'v', 'c', 'C' )" codecs/hevc.c
        grep -Fq 'lsmash_isom_box_type_value' core/box.c
        grep -Fq 'lsmash_qtff_box_type_value' core/box.c
        grep -Fq 'return isom_get_sample_group_description_common( list, ISOM_GROUP_TYPE_PROL );' core/isom.c
        grep -Fq 'return isom_get_sample_to_group_common( list, ISOM_GROUP_TYPE_PROL );' core/isom.c
        echo "Validated L-SMASH patch anchors"

        LSMASH_CFLAGS="${{ inputs.lsmash-extra-cflags }}"
        LSMASH_LDFLAGS="${{ inputs.lsmash-extra-ldflags }}"
        ./configure --prefix=${{ inputs.lsmash-install-prefix }} --cc=${{ inputs.lsmash-cc }} --extra-cflags="$LSMASH_CFLAGS" --extra-ldflags="$LSMASH_LDFLAGS"
        make clean
        make lib -j$(nproc)
        make install-lib

        mkdir -p lsmash-install/include lsmash-install/lib/pkgconfig
        cp /usr/local/include/lsmash.h lsmash-install/include/
        cp /usr/local/lib/liblsmash.a lsmash-install/lib/
        if [ -d /usr/local/lib/pkgconfig ]; then
          cp -R /usr/local/lib/pkgconfig/. lsmash-install/lib/pkgconfig/
        fi
        mv lsmash-install "$GITHUB_WORKSPACE/"

    - name: Install Cached L-SMASH
      if: inputs.use-lsmash == 'true' && steps.cache-lsmash.outputs.cache-hit == 'true'
      shell: msys2 {0}
      run: |
        cp -R lsmash-install/include/. /usr/local/include/
        cp -R lsmash-install/lib/. /usr/local/lib/

    - name: Verify L-SMASH Install
      if: inputs.use-lsmash == 'true'
      shell: msys2 {0}
      run: |
        test -s /usr/local/include/lsmash.h
        test -s /usr/local/lib/liblsmash.a

    - name: Compile GOP muxer
      if: inputs.use-gop-muxer == 'true' && steps.cache-gop-muxer.outputs.cache-hit != 'true'
      shell: msys2 {0}
      working-directory: ${{ inputs.gop-muxer-path }}
      run: |
        git -c core.autocrlf=false reset --hard HEAD
        test -f gop_muxer.cpp
        echo "Validated GOP muxer patch targets: gop_muxer.cpp"
        git apply --check ${{ inputs.gop-muxer-patch-path }}
        git apply ${{ inputs.gop-muxer-patch-path }}
        git diff --check -- gop_muxer.cpp
        grep -Fq 'lsmash_add_box(lsmash_root_as_box(p_root), free_box)' gop_muxer.cpp
        echo "Validated GOP muxer patch anchors"
        c++ -O2 --std=gnu++20 -I/usr/local/include -c gop_muxer.cpp -o gop_muxer.o
        c++ -static -static-libgcc -static-libstdc++ -L/usr/local/lib -o gop_muxer.exe gop_muxer.o -llsmash -lobuparse -lm
        mkdir -p /usr/local/bin gop-muxer-install/bin
        install -v gop_muxer.exe /usr/local/bin/gop_muxer.exe
        cp /usr/local/bin/gop_muxer.exe gop-muxer-install/bin/
        mv gop-muxer-install "$GITHUB_WORKSPACE/"

    - name: Install Cached GOP muxer
      if: inputs.use-gop-muxer == 'true' && steps.cache-gop-muxer.outputs.cache-hit == 'true'
      shell: msys2 {0}
      run: |
        cp -R gop-muxer-install/bin/. /usr/local/bin/

    - name: Verify GOP muxer Install
      if: inputs.use-gop-muxer == 'true'
      shell: msys2 {0}
      run: test -s /usr/local/bin/gop_muxer.exe
